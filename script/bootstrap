!#/bin/bash

# node > 0.10.x
node --version

# ruby > 1.8.7
ruby --version
gem --version

# sass > 3.3.x
sass --version

# nvm?
if hash nvm 2>/dev/null; then
  nvm use
fi

# gulp
npm install --global gulp

# dependencies
npm install
